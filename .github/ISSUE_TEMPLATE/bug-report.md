---
name: Bug report
about: Something is broken or behaving incorrectly
title: "[bug] "
labels: bug
---

<!-- Do not include secrets, passwords, or personal data. Screenshots are
fine, but blur anything private. -->

## Expected behavior

What should happen.

## Actual behavior

What happens instead. If it errors, paste the error text (an arae of a
stack trace is fine — do not paste user data).

## Steps to reproduce

1. Go to …
2. Tap …
3. …

## Environment

- Browser + version:
- OS / device:
- App version (`Settings → account` shows export info; or the hash on the
  `/account` page):
- Install method: PWA installed / normal browser tab / dev server

## Relevant logs

Only if available and non-sensitive (e.g. a screenshot of the browser
console). If the error is in a backup/restore flow, include the export log
(counts only) instead of any file contents.
