# Secrets are never required. This app is fully client-side and offline;
# there is no server, no database, no API key, and no credential.

# Optional: canonical site URL used for sitemap/robots and OpenGraph base.
# Format: https://your-domain.example  (no trailing slash)
# Required: no — production default is https://wird-gamma.vercel.app
# Security: not sensitive (public metadata only), but keep truthful to
# avoid leaking your staging domain in shared links.
NEXT_PUBLIC_SITE_URL=